<!DOCTYPE html>
<html>
<head>
	<meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
</head>
<body>
	<h1><%= @command_user.email %> a fait une commande!</h1>

	<p>Voilà leur commandes:</p>
	
	
	<% @cart_products_ordered.each do |product| %>
	<h2>
		<%= product.title %>
	</h2>
	<p>
		<%if product.picture.attached?%>
            <%= image_tag product.picture, alt: 'picture', class:"card-img-top", height:"300px" %>
        <%else%>     
		<img src="<%= product.image_url%>" width="600" height="auto" alt="<%= product.title%> image">
        <% end %></p>
	<% end %>

	<p>
		Une nouvelle commande a été faite !
	</p>

</body>
</html>